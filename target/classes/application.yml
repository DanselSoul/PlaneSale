
spring:
  datasource:
    url: "jdbc:postgresql://localhost:5432/planesale"
    username: "root"
    password: "course"
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      dialect: "org.hibernate.dialect.PostgreSQLDialect"
      ddl-auto: "create-drop"
#    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect